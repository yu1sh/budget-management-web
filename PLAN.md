# 公開・セルフホスト対応 家計簿／医療費記録サイト

## 概要

- Django、SQLite、Docker Compose、Caddyによる1世帯・管理者1人向けのセルフホストWebアプリ。
- GitHub公開を想定し、実データ、CSV、SQLite、バックアップ、秘密鍵、ログをリポジトリに含めない。
- ログイン後は必ず「家計簿」か「医療費控除」を選ぶ画面から開始する。

## 実装する機能

- 家計簿は月単位の日付・種別・店名・内訳・金額・支払い元・備考で、同日の複数明細を保存・編集・論理削除・CSV出力する。共通の日付・店名・支払い元に複数の「内訳＋金額」行を追加し、1回でまとめて登録できる。
- 支払い元はカード、コード決済、ポイント、銀行、現金・その他、交通系、フリマを利用者が管理する。コード決済はカード・銀行・現金を必須で、カードは銀行を任意で引き落とし元に設定でき、入力時の直接／最終引き落とし元と経路をスナップショット保存する。
- 支払い元は厳密な`#RRGGBB`イメージカラーを持ち、支払い元別カード・詳細表示・銀行管理へ安全に反映する。履歴または現行リンクを持つ支払い元は論理削除して履歴を保持し、未使用のみ物理削除する。
- 「クレジットカード引落明細」は支払月を基準にカードの締め日・支払い日・当月／翌月／翌々月を計算し、月末・閏年・28〜31日を処理する。直接カード利用とカード連携コード決済を一度だけ表示し、範囲印刷・CSV出力・未設定案内を提供する。
- 「引き落とし元別」は最終引き落とし元で通常支出を一度だけ集計し、コード決済→カード→銀行などの経路を明示する。未設定のコード決済は別途案内し、フリマ利益・出金は含めない。
- 銀行は既存のPaymentSource（銀行種類）を専用管理画面から追加・編集・利用停止・メモ管理し、カードの引き落とし先候補には利用中の銀行だけを使う。停止済み銀行は既存設定の表示・維持に限って許可する。
- フリマは家計簿の共通入力で利益・出金を日付・店名・内訳・金額・支払い元・備考付きで記録する。フリマの支払い元詳細も通常の家計簿明細テーブルを使い、月別利益・出金と全期間の利益−出金による残高目安を読取専用で表示する。フリマ明細は家計簿支出合計へ混ぜず、論理削除・CSV・範囲選択印刷に対応する。
- 支払い元詳細は読取専用。全体家計簿の同じ明細を直接利用またはコード決済経由として参照し、全体では重複計上しない。
- 医療費では年別の対象者を選び、対象者ページで既存病院を選択または病院名を追加してから、病院専用ページで日付・病院金額・薬局・交通費を入力する。1回の入力を受診記録（親）として保存し、薬局・交通費は選択した病院に付随する区分付き明細（子）とする。病院名を追加する操作だけでは空の受診記録を作らない。
- 医療費は人・年ごとに「日付、病院名/薬局名/交通手段、金額、操作」の4列で受診記録を表示し、編集・論理削除・CSV出力する。病院名から病院別ページへ移動し、付随する薬局・交通費を含む明細と合計を閲覧できる。
- 上部ナビゲーションは「医療費」に集約する。対象者ページで同名病院をまとめた合計カードを表示し、病院名なしの旧単独記録は通常の病院集計に混ぜず、受診記録一覧への案内として別表示する。旧病院別URLは医療費ページへリダイレクトする。
- 医療費の累計は受診日順に再計算し、人別合計と全員合計を表示する。旧明細は安全に単独受診記録へ移行し、病院との関係は推測しない。
- 対象表で矩形セル範囲をドラッグ（またはShift+矢印）選択し、選択列の見出しとセルのみをローカルのブラウザー印刷ダイアログで印刷する。

## 運用・安全性

- SQLiteを正本とし、変更成功後にUTF-8 BOM付きのCSVを原子的に再生成する。CSV数式注入を無害化する。
- `.env`はセットアップ時に0600で作成、管理者パスワードは対話入力しArgon2ハッシュのみ保存する。
- CaddyでHTTPS、セキュリティヘッダー、CSRF、Secure/HttpOnly/SameSite Cookie、12時間セッション、5回/15分ログインロックを設定する。
- 日次03:00（Asia/Tokyo）のローカルバックアップを30日保持し、復元スクリプトを提供する。
- 外部分析、CDN、クラウド保存、外部PDF生成を使用しない。
